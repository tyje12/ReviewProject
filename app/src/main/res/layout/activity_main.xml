<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.chrisbennett.mylistview.MainActivity"
    android:background="@drawable/bey">

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Album Reviewer"
        android:layout_alignParentEnd="true"
        android:fontFamily="Georgia"
        android:textSize="30dp"
        android:textAlignment="center"
        android:id="@+id/textView"
        android:focusableInTouchMode="false"
        android:textStyle="bold"
        android:textColor="#e322e7" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View"
        android:id="@+id/btnView"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="99dp"
        android:onClick="viewRecords" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ADd"
        android:id="@+id/btnAdd"
        android:layout_below="@+id/btnView"
        android:layout_alignStart="@+id/btnView"
        android:layout_marginTop="88dp"
        android:onClick="addRecord" />
</RelativeLayout>
